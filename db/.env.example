# Its own database, not storehub_recon -- same server is fine.
# PostgreSQL 17 is on port 5432 on this machine (18 is installed on 5433).
DATABASE_URL=postgresql://postgres:PASSWORD@127.0.0.1:5432/sales_analytics
